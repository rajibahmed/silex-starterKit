require 'rubygems'
require 'railsless-deploy'

load 'config/deploy' # remove this line to skip loading any of the default task

namespace :asset do
    desc "compress assets"
    task :compress do

    end
end


namespace :composer do
    desc "copy vendor directory"
    task :copy  do

    end

    desc "install php libraries"
    task :install  do

    end


    desc "update composer"
    task :update do

    end
end
